{
  "name": "@allthethings/phabricator-schema",
  "version": "1.0.0",
  "description": "AllTheThings Phabricator schema.",
  "repository": "Mossop/allthethings",
  "license": "MIT",
  "author": "Dave Townsend <dtownsend@oxymoronical.com>",
  "types": "dist/index",
  "scripts": {
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "depcheck": "depcheck",
    "build-client": "yarn build",
    "build-server": "yarn build",
    "build": "tsc -b --incremental",
    "watch": "tsc -b -w --incremental"
  },
  "dependencies": {
    "conduit-api": "^1.0.0"
  },
  "devDependencies": {
    "depcheck": "^1.4.2",
    "typescript": "^4.1.0"
  }
}
