query ListContextState {
  user {
    id
    email

    ...rootFields

    contexts {
      id
      stub
      name

      ...rootFields
    }
  }
}

query ListTaskList($taskList: ID!) {
  taskList(id: $taskList) {
    remainingTasks

    items {
      ...itemFields
    }

    sections {
      id
      name
      remainingTasks

      items {
        ...itemFields
      }
    }
  }
}
