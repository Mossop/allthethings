query ListContextState {
  user {
    email

    id
    subprojects {
      id
    }
    projects {
      id
      stub
      name
      subprojects {
        id
      }
      sections {
        id
        name
      }
    }

    contexts {
      stub
      name

      id
      subprojects {
        id
      }
      projects {
        id
        stub
        name
        subprojects {
          id
        }
        sections {
          id
          name
        }
      }
    }
  }
}
