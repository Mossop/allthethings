query ListContextState {
  user {
    email

    id
    subprojects {
      id
    }
    projects {
      id
      stub
      name
      subprojects {
        id
      }
      sections {
        id
        name
      }
    }

    contexts {
      stub
      name

      id
      subprojects {
        id
      }
      projects {
        id
        stub
        name
        subprojects {
          id
        }
        sections {
          id
          name
        }
      }
    }
  }
}

query ListTaskList($taskList: ID!) {
  taskList(id: $taskList) {
    items {
      id
    }

    sections {
      id
      name

      items {
        id
      }
    }
  }
}
