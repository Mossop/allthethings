{
  "name": "@allthethings/jira-schema",
  "version": "1.0.0",
  "description": "AllTheThings Jira schema.",
  "repository": "Mossop/allthethings",
  "license": "MIT",
  "author": "Dave Townsend <dtownsend@oxymoronical.com>",
  "types": "dist/index",
  "scripts": {
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "depcheck": "depcheck",
    "build-client": "yarn build",
    "build-server": "yarn build",
    "build": "tsc -b --incremental",
    "watch": "tsc -b -w --incremental"
  },
  "dependencies": {},
  "devDependencies": {
    "depcheck": "^1.4.2",
    "typescript": "^4.1.0"
  }
}
