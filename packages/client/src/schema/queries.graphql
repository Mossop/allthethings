query ListContextState {
  user {
    email

    id
    subprojects {
      id
    }
    projects {
      id
      stub
      name
      subprojects {
        id
      }
      sections {
        id
        name
      }
    }

    namedContexts {
      stub
      name

      id
      subprojects {
        id
      }
      projects {
        id
        stub
        name
        subprojects {
          id
        }
        sections {
          id
          name
        }
      }
    }
  }
}
